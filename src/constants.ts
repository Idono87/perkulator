export const FP_ADD = 'ADD';
export const FP_REMOVE = 'REMOVE';

export const DEFAULT_WATCH_PATH = './';
export const DEFAULT_EXCLUDE = '**/node_modules';

export const DEFAULT_CLEAN_LOG = false;
export const DEFAULT_LOGGING_LEVEL = 'info';
export const DEFAULT_SILENCE_LOG = false;

export const DEFAULT_CONFIG_PATH = '.perkulatorrc.json';

export const LOG_LEVELS = [
    'verbose',
    'debug',
    'info',
    'warn',
    'error',
    'fatal',
];
