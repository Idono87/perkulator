export const enum TaskEventType {
  error = 'error',
  update = 'update',
  result = 'result',
  skipped = 'skipped',
  stop = 'stop',
}

export const enum TaskProcessEventType {
  ready = 'ready',
}
